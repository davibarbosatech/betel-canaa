# Betel Canaã - Loja Virtual (Backend + Frontend)

Sistema de E-commerce completo com Painel Administrativo, Upload de Imagens e Banco de Dados.

## 📋 O que está aqui
- **Backend:** Node.js + Express + SQLite.
- **Frontend:** HTML/CSS puro (integrado na pasta `public`).
- **Recursos:**
  - API REST para produtos e pedidos.
  - Upload de imagens local (Multer).
  - Autenticação JWT (Login Admin/Cliente).
  - Banco de Dados SQLite (arquivo `database.sqlite`).

## 🚀 Instalação e Execução

1. **Instale o Node.js** (versão 14 ou superior).
2. Abra o terminal na pasta do projeto e instale as dependências:
   ```bash
   npm install